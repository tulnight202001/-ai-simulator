# AI Simulator 第一版實作計畫

**文件版本：0.2.0-alpha｜更新日期：2026-07-27｜程式基準 Commit：bb9d072｜目前階段：第二階段部分完成**

本計畫以 `GAME_SPEC.md` 為完整第一版範圍；目前的垂直切片只是第一階段的架構驗證，**不是最終交付**。

## 技術與架構

- **平台**：Vite + TypeScript + Phaser 3，手機優先的 PWA，靜態部署。
- **邊界**：`data/` 保存模型、工作區、配方、客戶與關卡；`core/` 保存純規則；`game/` 負責 Phaser 場景、輸入與呈現；`services/` 負責 IndexedDB、備份及平台能力。
- **可重現性**：每個生涯保存 seed 與亂數狀態；關卡與任務生成只透過種子亂數。
- **存檔**：版本化 IndexedDB，多模型存檔；所有持久資料經 migration 邊界存取。後續加入 JSON 預覽／匯入及選配 Google Drive adapter。
- **即時遊戲**：固定斜上方場景；鍵盤移動與點擊設備共用角色導航／互動狀態機；設備與資料箱都是場景物件而非表單。
- **離線**：service worker 快取 app shell 與資產；核心內容不呼叫 AI 或付費 API。

## 可驗證階段

### 1. 可玩垂直切片（本次）

**狀態：部分完成。** 程式功能已建立；因套件下載受阻，正式建置、公開 Preview、iPhone 與斷網重啟尚未驗證。

- 主選單、種子化模型三選一。
- 一個 180 秒工作站：櫃台、文字台、繪圖板、程式電腦；WASD／方向鍵與點擊尋路。
- 實體資料箱、單步及跨兩區配方、短時間設備加工、交付與一至三星結算。
- CPU／GPU／RAM／Context／伺服器負載與過載減速；IndexedDB 自動存檔；PWA app shell 離線。
- 純規則單元測試、資料完整性測試與 production build。

### 2. 核心系統完備

**狀態：部分完成。** 47 關、完整工作區／配方／客戶／升級資料與 version 2 生涯核心已建立；即時場景整合、決策、追加返工、完整管理 UI 尚未完成。

- 接受／追問／限制提示／替代／拒絕、客戶性格、追加、返工、抱怨與滿意度。
- 搜尋、文件、音樂、錄音、攝影、剪輯、部署等全部工作區；重複加工、並行占用與不可中斷。
- 多存檔、能力／設備／硬體升級、成長上限、重玩獎勵上限、備份 migration。

### 3. 五紀元正式內容

- 依資料表完成約 47 關（7 + 10 + 10 + 10 + 10），逐紀元加入雙步、多步、大型專案、返工及高負載目標。
- 建立全部工作站模組化地圖、客戶池、配方池、特殊目標與平衡基準；不得以垂直切片冒充完成。
- 補齊統一原創美術、動畫、Web Audio 音效與素材授權紀錄。

### 4. Agent、結局與長期模式

- Agent 指派、能力、資源占用與錯誤；不可取代大型專案的玩家決策。
- 生涯總評、正式通關、基礎無限模式、另一模型新生涯。
- JSON 備份／還原與衝突預覽；評估 Google Drive 選配備份的 OAuth 審查與替代方案。

### 5. 發布驗收

- 完成規格第 34 節所有文件，逐條對照第 36 節。
- 自動化功能、資料、migration、平衡 smoke tests；iPhone 真機觸控／安全區／背景返回；桌機；離線重啟；30/60 FPS 壓力測試。
- 靜態 Preview 部署、快取版本策略、資產壓縮、無障礙與正式授權審核。

## Definition of Done

只有五個階段全部完成、約 47 關／養成／Agent／總評／無限／備份／離線與手機驗收皆具證據時，才稱為第一版完成。
