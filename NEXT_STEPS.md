# NEXT STEPS

更新：2026-07-30。v3 客戶／11 工作站、彩色幾何訂單與資料夾、錯站靜默、下一站答案隱藏、快取修正與五紀元首關試玩已接入；390×844 視覺檢查及 22/22 test、check、build 已通過，完整第一版仍未達 `GAME_SPEC.md` 驗收標準。

## 已完成基準（不要重做）

- 首頁／選角 AI 手機尺寸與 service worker 更新策略已修正；AI／背景 v2 可與客戶／工作站 v3 混用。
- 訂單與資料夾以工作站色彩及幾何圖示讓玩家自行判讀；錯站不給答案或懲罰，中間步驟也不顯示下一站文字。
- Era 1–5 首關試玩不寫入生涯；後續修改不得破壞此隔離。

## 下一個任務可直接續作

1. 先以公開測試版完成 iPhone 真機的觸控熱區、字級、排隊辨識與 Era 5 效能回饋；只修正實際問題，不做重複驗證。
2. 在 `src/core/v1Runtime.ts` 與 `WorkstationScene` 完成**真正可切換的多資料夾攜帶／暫存架**：依 `caseSlots` 顯示槽位、允許選單、每份各自保存階段與客戶綁定。
3. 將客戶流程深化為資料驅動狀態機：矛盾需求、品質不足返工、投訴原因與滿意度明細；保留目前個別耐心、排隊離場與單次追加邊界。
4. 為 Agent 加入 catalog 數值（速度、品質、錯誤率、RAM/server 負載），限定單步簡單任務並讓錯誤產生可返工成果。
5. 以平衡模擬驗證 47 關星級門檻、獎勵與每紀元解鎖，再完成教學、暫停、設定／聲音、總評與無限模式；未達標不得宣稱完整 v1。

每個有意義的程式批次各執行一次 `npm test`、`npm run check`、`npm run build`；不要重複讀寫存檔或反覆跑相同驗證。需要發布文件時再同步 `/docs`、`/exports/google-drive` 與交接文件。
