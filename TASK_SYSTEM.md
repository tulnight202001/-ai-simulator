# 任務系統

`src/data/v1Catalog.ts` 是關卡、客戶、工作區與配方的資料來源；`src/core/v1Runtime.ts` 是接單、工序、佇列、交付、追加與 Agent 的規則核心。Phaser 場景不得直接寫死配方或另建一套判定。任務生成必須使用存檔 seed 與保存的亂數狀態。

## 配方與步驟

- 每個配方都要明確標記 `ordered` 或 `flexible`。
- `ordered` 只能完成下一個固定步驟；`flexible` 可完成任一尚未完成且工作區相符的步驟。
- 完成狀態依步驟 ID／index 記錄，不能以「完成數量」推測；重複使用相同工作區的步驟仍須分開。
- 點錯工作區完全靜默：不提示、不震動、不播放錯誤音效、不扣分、不改負載，也不顯示正確工作區或下一站文字。

## 訂單與五種接單決策

- 頂部訂單卡保持簡潔，但必須可點開完整內容；內容包含目的、動作、成果、固定／自由工序、完整步驟圖示、完成狀態、耐心及已知報酬／品質條件。
- 接受原單保留完整路線；追問消耗耐心並揭露規格、提高每步品質；限制委託縮短一個前置步驟並降低報酬／門檻；替代方案改走後半段並調整報酬／門檻；拒絕釋放欄位並承受滿意度代價。
- 五個選項必須在選擇前顯示實際取捨，不能只是五段不同文案。

## 工作區、資料夾與 Agent

- 十一種工作區使用一致的大型功能圖示、明顯主色與不同輪廓；訂單卡、客戶資料夾、玩家資料夾及機台本體共用同一圖示語彙。
- 已接訂單可由玩家持有、放入暫存槽、交給 Agent 或鎖定在加工中的工作區；所有權與位置必須是明確狀態。
- Agent 是可見的虛擬機器人，能抱自己的資料夾跟隨玩家。玩家選擇資料夾與機台後下達高階指令，Agent 自動尋路；系統不可代選正確機台。
- 每台工作區使用獨立加工工作；Era 4 最多兩台、Era 5 最多三台同時運算。單一資料夾同時只能在一台工作區，開始後不可中斷或改派。
- 手機 9:16 直向是正式版型；電腦只增加鍵盤／滑鼠輸入別名，不另建規則較有利的介面。
