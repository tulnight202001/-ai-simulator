# Codex Cloud 初始交付任務

請完整閱讀 `GAME_SPEC.md`，並以該文件作為本專案第一版的最高開發基準。

## 目前任務

從空白 Repository 建立《關於我重生為 AI 模型的⋯》（AI Simulator）第一版專案。

這不是只做 UI Mockup、技術 Demo、少量關卡 Prototype 或靜態展示。最終交付必須能在手機與電腦瀏覽器實際遊玩，並具備完整核心循環、模型養成、正式關卡、生涯總評、基礎無限模式、存檔與離線能力。

## 工作方式

1. 先閱讀並分析整份 `GAME_SPEC.md`。
2. 建立 `IMPLEMENTATION_PLAN.md`，將工作拆成可驗證階段，但不得刪減第一版必要內容。
3. 建立專案骨架與所有資料驅動 schema。
4. 先完成一個真正可玩的垂直切片，驗證：
   - 斜上方 2.5D 工作站。
   - 電腦移動與手機點設備尋路。
   - 櫃台接單。
   - 實體資料箱。
   - 至少三個工作區。
   - 單步及多步任務。
   - 關卡時間與結算。
   - CPU／GPU／RAM／Context／伺服器負載。
   - 本機存檔與 PWA 離線。
5. 垂直切片確認架構可擴充後，完成五個 AI 紀元與正式第一版。
6. 每個階段執行測試，不可到最後才補測試。
7. 維護 `CURRENT_STATE.md`、`TEST_REPORT.md` 與 `HANDOFF.md`。
8. 所有關卡、模型、任務、工作區與客戶數值必須資料驅動，不可散落硬寫在 UI 程式中。
9. 核心遊戲不得依賴真實 AI API 或付費服務。
10. 遇到技術阻塞時，先記錄具體原因、影響與替代方案；不得自行刪除規格。

## 技術方向

優先評估：
- TypeScript
- Phaser 或同等成熟開源 2D Web 遊戲引擎
- PWA Service Worker
- IndexedDB
- Web Audio
- 種子化亂數
- JSON／CSV／TypeScript 資料驅動設定
- 免費靜態部署

選單與管理頁可使用一般 Web UI，但即時關卡核心不得做成表單式網頁。

## 第一個可見交付

第一個可見版本必須能透過公開 Preview／Deployment URL 在 iPhone 上測試，至少包含：
- 主選單。
- 三個模型三選一。
- 一個小型工作站。
- 一名可移動 AI。
- 櫃台、文字台、繪圖板、程式電腦。
- 客戶任務與資料箱。
- 單步任務。
- 一個跨兩個工作區的任務。
- 約三分鐘的關卡。
- 時間結算與一至三星。
- CPU／GPU／RAM／Context 負載。
- 關卡結束後本機自動存檔。
- PWA 安裝與離線重新開啟。

第一個可見版本不是第一版最終交付；完成後必須依 `GAME_SPEC.md` 繼續開發完整內容。

## 禁止事項

- 不可把遊戲縮成純文字卡片選項。
- 不可只做工程後台或資料儀表板。
- 不可用像素風、復古風或低多邊形 3D。
- 不可把料理／廚房美術直接套進遊戲。
- 不可使用未確認授權的商標、Logo、角色或素材。
- 不可因工作量大而擅自刪除正式關卡、生涯總評、無限模式、離線或存檔。
- 不可只以「能啟動」「能部署」作為完成標準。

## 最終驗收

逐項對照 `GAME_SPEC.md` 第 36 節完成交付標準，並在 `TEST_REPORT.md` 提供證據。
