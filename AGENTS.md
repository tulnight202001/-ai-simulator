# Codex 開發指示

本 Repository 的第一版開發以 `GAME_SPEC.md` 為最高規格基準。

開始任何程式工作前，依序閱讀：

1. `CODEX_TASK.md`
2. `GAME_SPEC.md`
3. `HANDOFF.md`
4. `CURRENT_STATE.md`（建立後）
5. `TEST_REPORT.md`（建立後）

必須遵守：

- 不得把遊戲縮成文字選項、靜態 Mockup、工程後台或少量技術 Demo。
- 第一個任務先建立可測試的垂直切片與完整可擴充架構，但不得把垂直切片當成第一版最終完成。
- 手機優先，同時支援電腦；即時關卡需使用適合遊戲的架構。
- 核心遊戲不得依賴真實 AI API、付費服務或強制登入。
- 所有模型、客戶、任務、關卡、工作區與平衡數值必須資料驅動。
- 完成任何工作前都要執行可用的測試，並更新 `CURRENT_STATE.md`、`TEST_REPORT.md` 與 `HANDOFF.md`。
- 遇到技術阻塞時，記錄原因、影響與替代方案，不得自行刪除規格。
- 所有變更應提交至 Git，並保持工作區乾淨。

第一個可見交付請依 `CODEX_TASK.md` 的「第一個可見交付」執行。
