# Current state — 2026-07-27

**目前版本：0.2.0-alpha｜更新日期：2026-07-27｜程式基準 Commit：60037d5**

**整體第一版估計完成度：約 22%｜目前階段：核心資料與生涯架構部分完成｜不是完整第一版**

## 文件與備份（已完成）

- GitHub 維持程式碼、規格及版本歷史的主要備份來源。
- `/docs/index.html` 是適合手機閱讀的專案狀態入口；可由 GitHub Pages 免費發布。
- `/exports/google-drive` 保存由同一批 Markdown 自動產生的離線 HTML，可由使用者手動上傳 Google Drive，不需登入或保存 OAuth Token。
- 階段 ZIP 不再提交到 Git。`.github/workflows/release-archive.yml` 會產生 `ai-simulator-v0.2.0-alpha-20260727.zip` 並保存為 GitHub Actions artifact；內容不含依賴、快取、建置暫存或憑證。

## 本階段新增（部分完成）

- 建立五紀元共 47 關的資料表、11 種正式工作區、12 種單步至完整歌曲與 MV 配方、6 種客戶及升級資料；尚未全部接入即時 Phaser 場景。
- 建立多模型 IndexedDB 生涯 schema version 2、舊版 migration、星級最佳成績、有限重玩獎勵、長期數值、升級、Agent／無限／總評資料結構。
- 主選單可讀取多個生涯，模型三選一包含約 10% 稀有模型機率，結算會寫入長期生涯；JSON 可下載，匯入預覽核心已有但 UI 尚未接上。
- PR #1 的 GitHub Actions 已在官方依賴環境完整通過：`npm install`、`npm test`、`npm run check`、`npm run build` 均成功，並產生 production build artifact。
- TypeScript 失敗原因已確認為 Phaser 第三方宣告檔引用舊式 `ActiveXObject` 全域型別；以標準 `skipLibCheck` 忽略第三方宣告檔相容性問題，同時保留專案原始碼的 strict 型別檢查。

## 已完成（第一個可見垂直切片）

- 建立 Vite／TypeScript／Phaser 3 的可擴充 PWA 架構與五階段 `IMPLEMENTATION_PLAN.md`。
- 主選單、三個有取捨的原創模型三選一及固定外觀。
- 固定斜上方小型工作站；櫃台、文字台、光繪板、程式電腦；鍵盤移動、點設備尋路與大型觸控互動按鈕。
- 種子化單步／雙步任務、實體資料箱流程、短時間不可中斷加工、180 秒倒數、交付、分數與一至三星結算。
- CPU／GPU／RAM／Context／伺服器 HUD；設備消耗、閒置恢復及伺服器過載減速。
- 完成關卡後 IndexedDB 自動存檔；PWA manifest、service worker app-shell/runtime cache。
- 純規則與資料參照測試原始碼，以及架構、schema、控制、離線文件。

## 可測試方式

在可使用 npm 的環境執行 `npm install && npm run dev`，瀏覽 Vite URL；選模型後到櫃台領箱，依 HUD 先後前往設備，最後回櫃台。可把時間暫改或完整等待 180 秒查看結算與 IndexedDB `ai-simulator`。

GitHub Actions 已證明依賴安裝、自動測試、TypeScript 檢查及 production build 均可成功執行。Codex 容器本身仍會對 npm registry 回應 HTTP 403，但不再阻塞 Repository 的正式 CI 驗證。

### 測試網址與文件網址

- 遊戲 Preview：**尚未產生公開網址**；production build artifact 已成功建立。
- 文件首頁：目前可直接開啟 `/docs/index.html`；GitHub Pages 公開網址 **尚未啟用**。
- 啟用方式：Repository 管理者進入 **Settings → Pages → Build and deployment → GitHub Actions**；推送後由 `.github/workflows/docs-pages.yml` 發布遊戲與 `/docs`。公開後應把實際網址補回本節。

## 尚未完成（不可視為第一版完成）

- 接單分支（追問／拒絕／替代）、程序化客戶、追加／返工／抱怨，以及完整工作區與大型專案。
- 約 47 個正式關卡、五紀元、全部模組地圖、正式平衡與完整教學。
- 模型／設備／硬體升級、Agent、長期數值、多存檔 UI、獎勵上限。
- 生涯總評、基礎無限模式、JSON 備份／migration UI、選配 Google 備份。
- 正式美術／聲音與授權清冊、暫停／設定／減少動畫、iPhone 真機與離線重啟驗收。

## 下一階段

GitHub Pages 已存在，GitHub Actions 的安裝、測試、TypeScript 檢查與 production build 已通過。正式 47 關資料與所有工作區現已接入即時 Phaser 場景；完整 v1 仍需依 `NEXT_STEPS.md` 完成多箱互動、完整返工／投訴、平衡及其餘驗收畫面。

## 響應式工作站視覺重製

- 工作站畫面已加入安全區感知的全螢幕外框，桌機顯示鍵盤提示，窄版直向手機顯示旋轉建議；Phaser 的固定邏輯座標與 `FIT` 縮放確保玩法座標不因版面尺寸改變。
- 2.5D 場景已重製為分層青藍工作站：地板邊界、透視網格、設備發光層、玩家陰影與高對比 HUD 更容易辨識，原有資料箱、工作區、計時與資源規則均保留。
- 本次只重製既有垂直切片的視覺與響應式呈現，沒有宣稱五紀元第一版內容完成。

## 本回合：生涯管理 UI（2026-07-27）

- 主選單現在可匯入 JSON 備份、先預覽模型／更新時間並確認覆蓋，也可明確確認後刪除個別生涯存檔。
- 生涯中心已接上資料驅動升級商店：依紀元顯示可用升級、等級、效果、代價、動態價格與資源不足／滿級狀態，購買後立即保存。
- 已解鎖的 Agent 可選擇文字、繪圖、程式單步任務或休息，指派會持久化；指定類型的單步任務已能在 Phaser 場景自動處理並受忙碌與冷卻限制。
- 完整第一版仍未完成；47 關與完整工作區的即時整合仍為最高優先。

## 2026-07-28 本次整合

- 正式 47 關已具備五紀元關卡選擇、逐關星級解鎖與完成狀態；GitHub Pages 已存在。
- `v1Levels`、11 種 `v1Workstations` 與單／雙／多步／大型 `v1Recipes` 已轉為 Phaser 執行期資料，所有正式工作區可在關卡地圖互動。
- 資料箱保存階段成果並阻擋錯站；設備一旦開始便鎖定至完成。
- 櫃台具接受、追問、提醒限制、替代方案、拒絕，以及交付後追加接受／拒絕與滿意度結果。
- 硬體、Context、穩定度、額外資料槽和 Agent 等級已接入執行期；Agent 僅自動處理指定的文字／繪圖／程式單步工作且有忙碌與冷卻限制。
- 既有多存檔、JSON 匯入／預覽／覆蓋／刪除、生涯、升級與 Agent UI 均保留。GitHub Actions 已可正常 install、test、check、build。

## 2026-07-30 客戶 v3、排隊訂單與後期試玩

- 六種客戶已換成 2-B 方向的高畫質 2.5D Q 版人類，正式素材位於 `public/art/generated/v3/`；客戶在櫃檯後排隊，AI 與可操作工作站在前場，角色只做輕微待機浮動，不以拉伸圖片製造動態。
- 遊戲上方固定顯示三張圖示訂單卡、個別耐心條與總排隊數；畫面最多呈現五位客戶，較高紀元會加快到客速度並提高同時等待量。
- 接待後，客戶的需求資料夾會交到 AI 手上；資料夾以工作站色彩及幾何圖示顯示製作順序，完成後送回中央交付盤。
- 每個產品現在同時說明「目的、動作、成果」，不再只顯示語意不明的產品短文。
- 生涯中心與選關頁可直接啟動 Era 1–5 各自第一關。試玩模式繞過正式解鎖，但不寫入星級、獎勵、生涯進度或存檔。
- 最短測試入口：開啟遊戲 → 繼續任一存檔 →「五紀元首關試玩」→ Era 5。公開測試站為 `https://tulnight202001.github.io/-ai-simulator/`；在本批推送前仍會顯示上一版。
- 仍待後續：更多真機觸控與效能驗收、客流平衡、完整幀動畫、總評／無限模式與正式音效。現階段不可宣稱完整 v1 已完成。

## 2026-07-30 最終整合補記

- 首頁與選角 AI 的顯示容器已按手機直向重新校準，避免角色過小或裁切；開發模式會註銷舊 service worker 並在受控頁面重載，正式版改用 `ai-simulator-v5-20260730` 的 network-first 更新策略，離線時才回退快取。
- 六種客戶與全部 11 種工作站均使用 `public/art/generated/v3/` 正式素材；AI 角色與五紀元背景刻意保留相容的 v2 素材。
- 訂單卡、客戶資料夾、AI 手持資料夾與交付動畫已統一使用工作站專屬色與程式繪製的幾何圖示；單一步驟會置中，多步驟依序排列。
- 點錯工作站時只做無副作用驗證：不提示、不震動、不播放音效、不扣分；完成中間步驟後也不以文字揭露下一台工作站，只要求玩家對照資料夾的顏色與圖案。
- Era 1–5 首關試玩入口維持與正式生涯隔離；390×844 手機直向畫面已完成首頁、選角、訂單／排隊、資料夾、工作站與後期首關的視覺檢查。
- 最終驗證：`npm test` 22/22、`npm run check` 通過、`npm run build` 通過；建置僅保留既有 Phaser chunk size warning。

## 2026-07-30 已完成：任務資訊、機台辨識與 Agent 並行

- 十一種工作區已在機台本體上加入大型功能圖示，並使用固定、高差異主色與不同輪廓；390×844 的 Era 5 場景不需只靠小字即可辨識。
- 每張頂部訂單卡已可點開完整內容，顯示客戶、產品名稱、目的、實際動作、完成成果、`ordered`／`flexible`、全部步驟、完成狀態、耐心及已知報酬／品質條件。
- 固定／自由工序已完整接入核心與畫面；自由工序的完成勾選改依步驟 ID／index 判斷，不會因先完成後項而誤勾前項。
- 五種接單決策的選擇畫面已能預覽實際影響：接受保留原單；追問扣耐心並取得完整規格與品質加成；限制縮短前置工序並降低報酬／門檻；替代保留後半路線；拒絕釋放欄位並降低滿意度。
- Agent 已改為場景中可見的虛擬機器人，能抱獨立資料夾跟隨玩家；玩家可交箱、選取 Agent、指派機台，完成後 Agent 會抱回資料夾，不會自動替玩家選正確機台。
- 每台機器已改用獨立加工工作；訂單卡會顯示玩家持有、Agent 持有或機台加工狀態。Era 4 上限兩台、Era 5 上限三台同時運算。
- 玩家與 Agent 點錯機台都走完全靜默分支：不提示答案、不震動、不播放錯誤音效、不扣分，也不改變工序與資源。
- 390×844 視覺冒煙已確認首頁與選角角色尺寸正常、無遮擋，Era 5 的機台、訂單詳情、五決策、Agent 跟隨／指派／抱回及多機狀態正常。
- 本批最終驗證為 `npm test` 27/27、`npm run check` 通過、`npm run build` 通過；建置僅有既存 Phaser chunk size warning。
- 尚待推送公開版後，由使用者以 iPhone 真機驗收觸控、字級、效能與實際網路載入；這不影響本機程式與 390×844 冒煙結果。
